<!DOCTYPE html>
<html>

<head>
    <title>Portfolio- Edição</title>
    <link rel="stylesheet" href="/stylesheets/bootstrap.min.css">
</head>

<body>
    <div class="container">
        <div class="row">
            <div class="col-sm-12 col-md-12 col-lg-12">
                <h1 class="text-center">Portfolio-Edição</h1>

                <div class="dropdown-divider"></div>
                <h3>Editar Portfolio!</h3>
                <form action="/port/edit" method="POST">
                    <div class="form-group">
                        <label for="title">Título:</label>
                        <input id="title" value="<%= inte.title %>" type="text" class="form-control" name="title" placeholder="Digite o título" />
                    </div>
                    <div class="form-group">
                        <label for="date">Data:</label>
                        <input id="date" value="<%= inte.date %>" type="date" class="form-control" name="date" placeholder="Digite a data" />
                    </div>
                    <div class="form-group">
                        <label for="description">Descrição:</label>
                        <input id="description" value="<%= inte.description %>" type="text" class="form-control" name="description" placeholder="Digite a descrição" />
                    </div>
                    <input type="hidden" name="id" value="<%= inte._id %>" />
                    <button type="submit" class="btn btn-primary text-center">Editar</button>
                </form>
            </div>
        </div>
    </div>
</body>
</html>