<!DOCTYPE html>
<html>

<head>
    <title>Interesses</title>
    <link rel="stylesheet" href="/stylesheets/bootstrap.min.css">
</head>

<body>
    <div class="container">
        <div class="row">
            <div class="col-sm-12 col-md-12 col-lg-12">
                <h1 class="text-center">Interesses</h1>

                <div class="dropdown-divider"></div>
                <h3>Pesquise por um interesse</h3>
                <form action="/interesse/get" method="post">
                    <div class="form-group">
                        <label for="obj">Buscar:</label>
                        <input id="obj" type="text" class="form-control" name="obj" placeholder="Buscar interesse" />
                    </div>
                    <button type="submit" class="btn btn-primary text-center">BUSCAR</button>
                </form>

                <div class="dropdown-divider"></div>
                <h3>Todos os Interesses cadastrados</h3>

                <div class="col-sm-12 col-md-12 col-lg-8">
                    <ul class="list-group">
                        <% if(!inte || inte.length == 0) {%>
                            <h4>Não existem interesses cadastrados</h4>
                        <% } else { %>
                            <% for (i of inte) { %>
                        <li class="list-group-item">
                            <%= i.informacao %>
                            <a href="/interesse/editarView/<%= i._id %>" class="badge badge-primary">Editar</a>
                            <a href="/interesse/delete/<%= i._id %>" class="badge badge-danger">Excluir</a>
                        </li>
                            <% } %>
                        <% } %>
                    </ul>
                </div>

                <div class="dropdown-divider"></div>
                <h3>Cadestre um novo interesse!</h3>
                <form action="/interesse/save" method="POST">
                    <div class="form-group">
                        <label for="interesse">Interesses:</label>
                        <input id="interesse" type="text" class="form-control" name="interesse" placeholder="Digite novo interesse" />
                    </div>
                    <button type="submit" class="btn btn-primary text-center">Salvar</button>
                </form>
            </div>
        </div>
    </div>
</body>

</html>