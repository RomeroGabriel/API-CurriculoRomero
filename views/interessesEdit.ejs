<!DOCTYPE html>
<html>

<head>
    <title>Interesses- Edição</title>
    <link rel="stylesheet" href="/stylesheets/bootstrap.min.css">
</head>

<body>
    <div class="container">
        <div class="row">
            <div class="col-sm-12 col-md-12 col-lg-12">
                <h1 class="text-center">Interesses-Edição</h1>
                <% if(!inte || inte.length == 0) {%>
                    <h4>Não existem o interesse buscado</h4>
                <% } else { %>
                <div class="dropdown-divider"></div>
                <h3>Editar interesse!</h3>
                <form action="/interesse/edit" method="POST">
                    <div class="form-group">
                        <label for="interesse">Interesses:</label>
                        <input id="interesse" value="<%= inte.informacao %>" type="text" class="form-control" name="interesse"
                            placeholder="Digite novo interesse" />
                    </div>
                    <input type="hidden" name="id" value="<%= inte._id %>" />
                    <button type="submit" class="btn btn-primary text-center">Editar</button>
                </form>
                <% } %>
            </div>
        </div>
    </div>
</body>

</html>